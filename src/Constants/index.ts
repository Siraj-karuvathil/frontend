
const local = 'http://localhost:3001';
const server = 'https://api.cookingacademy.qa';

const urls = {
    local,
    server,
};

const current = urls.local;

export const BASE_URL = `${current}/api/1.0/`;